# AMAS Seminary

This repository was recreated after the original source tree was deleted.

Current state:

- The original source files are missing.
- A runnable frontend bundle was recovered from the iOS simulator install.
- The recovered snapshot is stored under `recovered_from_simulator/public/`.

Purpose of this repository:

- Preserve the current recovery baseline.
- Track all future reconstruction work.
- Prevent accidental total loss of the workspace again.

Recommended next steps:

- Rebuild a new source workspace from the recovered bundle.
- Recreate `App.tsx`, `components/`, `services/`, and `backend/` incrementally.
- Add a remote backup repository after the local Git history is established.
